import { createEvent } from "effector";

// USER
export const resetCurrentUserEvent = createEvent();
export const resetUpdateUserEvent = createEvent();
export const resetUnitItemsEvent = createEvent();
export const resetProductItemsEvent = createEvent();
